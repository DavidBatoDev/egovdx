#:schema https://developers.openai.com/codex/config-schema.json

# Limit Codex to this development project. Schema writes are enabled for migrations.
# Codex reads the token from the process environment; no secret is committed.
[mcp_servers.supabase]
url = "https://mcp.supabase.com/mcp?project_ref=fvvqesqjerhtebdifhun"
bearer_token_env_var = "SUPABASE_ACCESS_TOKEN"
